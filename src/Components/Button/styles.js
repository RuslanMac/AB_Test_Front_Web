import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((Theme) => ({
  Button: {
    width: '120px',
    height: '38px',

    background: '#4A9DFF',
    borderRadius: '10px',
    borderWidth: '0px', 
    fontFamily: 'Ubuntu',
    fontStyle: 'normal',
    fontWeight: 'normal',
    fontSize: '14px',
    lineHeight: '16px',
    marginBottom: '15px',

    color: '#FFFFFF', 

  },
}));

export default useStyles;
